# No cache rule for all API versions and endpoints (deployed site was aggressively caching)
[[headers]]
  for = "/api/*/*"
  [headers.values]
    cache-control = '''
    no-store,
    no-cache,
    must-revalidate'''
    pragma = "no-cache"
    expires = "0"